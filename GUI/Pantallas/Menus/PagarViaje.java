package GUI.Pantallas.Menus;

import GUI.Pantallas.Pantalla;

import java.awt.*;

public class PagarViaje extends Pantalla {
    public PagarViaje(int pantalla, Color mainColor, Color secondary, Color mainBackgroundColor, long deltaTime, int windowWidth, int windowHeight, Container container) {
        super(pantalla, mainColor, secondary, mainBackgroundColor, deltaTime, windowWidth, windowHeight, container);
    }
}
